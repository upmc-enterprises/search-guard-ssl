general:
  branches:
    only:
      - do_not_build
      
machine:
  java:
    version: oraclejdk8

deployment:
  deploysnap:
    branch: master
    commands:
      - mvn deploy -DskipTests -s settings.xml