general:
  branches:
    only:
      - ci-only_openjdk8
      
machine:
  java:
    version: openjdk8

dependencies:
  pre:
    - sudo apt-get update; sudo apt-get install openssl libapr1

deployment:
  deploysnap:
    branch: master
    commands:
      - mvn deploy -DskipTests -s settings.xml